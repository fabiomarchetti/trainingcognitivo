/**
 * Layout per sezioni Dashboard (non Admin - Admin ha il suo layout)
 */

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
